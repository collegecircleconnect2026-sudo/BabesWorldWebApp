/** @type {import('next').NextConfig} */
const nextConfig = {
  // Nothing special is needed for the Vercel free tier — this site is fully static.
  reactStrictMode: true,
};

export default nextConfig;
